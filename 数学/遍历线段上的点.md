
## Templates

```java
for (int i = 0; i < n; i++) {  
	in.nextToken();  
	long ux = (long) in.nval;  
	in.nextToken();  
	long uy = (long) in.nval;  
	in.nextToken();  
	long vx = (long) in.nval;  
	in.nextToken();  
	long vy = (long) in.nval;  
	
	long dx = vx - ux, dy = vy - uy;  
	long g = gcd(Math.abs(dx), Math.abs(dy));  
	dx /= g;  
	dy /= g;  
	for (long j = 0; j <= g; j++) {  
		points.add((ux + j * dx << 31) | (uy + j * dy));  
	}  
}  

```

## Problems

[洛谷 P10907 [蓝桥杯 2024 国 B] 蚂蚁开会](https://www.luogu.com.cn/problem/P10907) 