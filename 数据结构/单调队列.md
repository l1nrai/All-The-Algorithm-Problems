
单调队列 = 单调栈 + 滑动窗口.

核心思想：及时移除无用数据，保证队列/栈的有序性.

什么是有用数据？即 **更年轻** 或 老但优秀 的数据.

## Templates

c
```c
int fun (int *a,int n,int k)
{
	// front为队首指针，rear为队尾的后一位指针
	// 当front==rear时，队列为空
    int ans = 0, l = 0;
    int maxQueue[n + 10], minQueue[n + 10];
    int maxFront = 0, maxRear = 0, minFront = 0, minRear = 0;
    for(int i = 0; i < n; i ++)
    {
	    // 入
        int x = a[i];
        while(maxFront != maxRear && a[maxQueue[maxRear - 1]] <= x)
            maxRear --;
        maxQueue[maxRear ++] = i;
        while(minFront != minRear && a[minQueue[minRear - 1]] >= x)
            minRear --;
        minQueue[minRear ++] = i;

		// 出
        while(a[maxQueue[maxFront]] - a[minQueue[minFront]] > k)
        {
            if(maxQueue[maxFront] == l)maxFront ++;
            if(minQueue[minFront] == l)minFront ++;
            l ++;
        }

		// 更新答案
        ans = (i - l + 1 > ans ? i - l + 1 : ans);
    }
    return ans;
}
```

## Problems

**模板**

- [力扣 239. 滑动窗口最大值](https://leetcode.cn/problems/sliding-window-maximum/) 
- [力扣 LCR 184. 设计自助结算系统](https://leetcode.cn/problems/dui-lie-de-zui-da-zhi-lcof/)

**子数组极值与滑动窗口**

- [力扣 1438. 绝对差不超过限制的最长连续子数组](https://leetcode.cn/problems/longest-continuous-subarray-with-absolute-diff-less-than-or-equal-to-limit/) 子数组越长，极值越大
- [力扣 2762. 不间断子数组](https://leetcode.cn/problems/continuous-subarrays/) 
- [力扣 3835. 开销小于等于 K 的子数组数目](https://leetcode.cn/problems/count-subarrays-with-cost-less-than-or-equal-to-k/) 
- [力扣 2398. 预算内的最多机器人数目](https://leetcode.cn/problems/maximum-number-of-robots-within-budget/)
- [洛谷 P2698 [USACO12MAR] Flowerpot S](https://www.luogu.com.cn/problem/P2698) 
- [洛谷 P12225 [蓝桥杯 2023 国 Java B] 游戏](https://www.luogu.com.cn/problem/P12225) 数学转换

- [力扣 3589. 计数质数间隔平衡子数组](https://leetcode.cn/problems/count-prime-gap-balanced-subarrays/) 进阶，**多重（相反）约束条件**

**进阶**

- [力扣 862. 和至少为 K 的最短子数组](https://leetcode.cn/problems/shortest-subarray-with-sum-at-least-k/) 前缀和
- [力扣 1499. 满足不等式的最大值](https://leetcode.cn/problems/max-value-of-equation/)